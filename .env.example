# MongoDB Atlas / local MongoDB connection. Copy this file to .env locally.
# In Vercel, add the same names under Project Settings → Environment Variables.
# Do not use quotes, square-bracket placeholders, or HTML-escaped &amp; here.
MONGODB_URI="mongodb+srv://dexterlambbb_db_user:DxSZ46sEYFk5PHmN@cluster0.wdrok43.mongodb.net/pos_system?retryWrites=true&w=majority&appName=Cluster0"
MONGODB_DB=face-scan

# Required for a Vercel deployment. Generate a long random private value.
SECRET_KEY=replace-with-a-long-random-secret

# Required on Vercel to protect student, face-profile, and attendance data.
APP_ACCESS_USERNAME=teacher
APP_ACCESS_PASSWORD=replace-with-a-strong-private-password
